% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_acs_data.R
\name{view_acs_data}
\alias{view_acs_data}
\title{View ACS data in a local Shiny app}
\usage{
view_acs_data(
  .data,
  variables = NULL,
  geography = NULL,
  target_geographies = NULL,
  ...
)
}
\arguments{
\item{.data}{An \code{sf} object produced by \code{\link[=compile_acs_data]{compile_acs_data()}} with
\code{spatial = TRUE}. Must retain its \code{"codebook"} attribute.}

\item{variables}{Optional character vector restricting which variables appear
in the picker. Accepts either raw column names (e.g.,
\code{"race_nonhispanic_white_alone_percent"}) or pretty labels as produced by
\code{\link[=make_pretty_names]{make_pretty_names()}}. When \code{NULL} (default), all eligible numeric
variables from the codebook are shown.}

\item{geography}{Geography level of \code{.data}, matching the value passed to
\code{\link[=compile_acs_data]{compile_acs_data()}}. When \code{"tract"}, \code{"county"}, or \code{"state"}, the
"Statistical benchmark" dropdown is shown with appropriate higher-level
geographies. Any other value (or \code{NULL}) hides the dropdown.}

\item{target_geographies}{Optional \code{sf} polygon object whose rows define
target geographies onto which the source data are interpolated via
\code{\link[=interpolate_acs]{interpolate_acs()}} using area-weighted overlap. Must contain a \code{GEOID}
column that uniquely identifies each polygon; an optional \code{NAME} column
is used in popups when present. When supplied, a "Geography" toggle
appears in the sidebar that switches the map between the source and the
target dataset. Benchmark values remain computed from the source data;
in Target view, each target polygon is mapped to a parent (county /
state / national) by majority-area overlap.}

\item{...}{Forwarded to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g., \code{port}, \code{host},
\code{launch.browser}).}
}
\value{
A \code{shiny.appobj} (invisibly). When called from an interactive
session, the app is launched in the default browser.
}
\description{
Launch a small, locally-run Shiny app that visualizes the spatial
output of \code{\link[=compile_acs_data]{compile_acs_data()}} as an interactive choropleth map. Colors,
breaks, and popups are derived from the codebook attribute attached to the
input.
}
\details{
The map uses MapLibre via the mapgl package and the CARTO Positron basemap
(no API token required). Color palettes default to those in the
\href{https://github.com/UrbanInstitute/urbnthemes}{urbnthemes} package
(sequential cyan for counts/medians/averages, quintile cyan for percentages,
diverging cyan/orange for index variables).

A variable picker is shown in the sidebar; if \code{.data} spans multiple years a
year picker is also shown. Below the picker an interactive histogram of the
selected variable is rendered, and users can drag a horizontal selection on
it to restrict the choropleth's color scale. Polygons whose value falls
outside the selected range render in neutral grey, and their popups note the
out-of-range status. The brushable range defaults to \code{c(0, 1)} for percent
variables and to the observed data range for everything else.

When \code{geography} is one of \code{"tract"}, \code{"county"}, or \code{"state"}, a
"Statistical benchmark" dropdown appears that recolors the map by whether
each polygon's estimate is statistically significantly larger than, smaller
than, or indistinguishable from a higher-geography benchmark. Benchmark
values are aggregated from \code{.data} via \code{\link[=interpolate_acs]{interpolate_acs()}} (so the result
may differ slightly from a separately-pulled ACS estimate at that geography),
and significance is tested via \code{\link[tidycensus:significance]{tidycensus::significance()}} at the
90 percent confidence level (matching the \emph{quantified survey error}
vignette). Variables without a margin-of-error column fall back to the
default coloring.

\code{view_acs_data()} requires the \code{shiny}, \code{mapgl}, \code{bslib}, and \code{ggplot2}
packages. These are listed in \code{Suggests} and checked at runtime.
}
\examples{
\dontrun{
df = compile_acs_data(
  tables    = c("race", "snap"),
  years     = 2022,
  geography = "tract",
  states    = "NJ",
  spatial   = TRUE)

view_acs_data(df, geography = "tract")

## Restrict the picker to a few variables:
view_acs_data(df, geography = "tract",
              variables = c("snap_received_percent",
                            "race_nonhispanic_white_alone_percent"))

## Interpolate onto custom target polygons (e.g., neighborhoods):
neighborhoods = sf::st_read("path/to/neighborhoods.geojson")
# neighborhoods must have a `GEOID` column identifying each polygon.
view_acs_data(df, geography = "tract",
              target_geographies = neighborhoods)
}
}
\seealso{
\code{\link[=compile_acs_data]{compile_acs_data()}}, \code{\link[=interpolate_acs]{interpolate_acs()}},
\code{\link[tidycensus:significance]{tidycensus::significance()}}
}
