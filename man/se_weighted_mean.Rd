% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_cvs.R
\name{se_weighted_mean}
\alias{se_weighted_mean}
\title{Calculate standard error for population-weighted mean}
\usage{
se_weighted_mean(
  values,
  weights,
  se_values = NULL,
  se_weights = NULL,
  moe_values = NULL,
  moe_weights = NULL
)
}
\arguments{
\item{values}{A numeric vector of values being averaged (e.g., median incomes)}

\item{weights}{A numeric vector of population weights}

\item{se_values}{Standard errors for the values (or NULL if providing moe_values)}

\item{se_weights}{Standard errors for the weights (or NULL if providing moe_weights)}

\item{moe_values}{Margins of error for the values (or NULL if providing se_values)}

\item{moe_weights}{Margins of error for the weights (or NULL if providing se_weights)}
}
\value{
The standard error of the weighted mean
}
\description{
Calculate standard error for population-weighted mean
}
\details{
Calculate the standard error for a population-weighted average,
used when aggregating median or average variables across geographies.
Uses a multi-step approach following Census Bureau guidance:
\enumerate{
\item Calculate SE for each product (value * weight) using se_product()
\item Calculate SE for the sum of products (numerator) using se_sum()
\item Calculate SE for the sum of weights (denominator) using se_sum()
\item Calculate SE for the ratio using se_proportion_ratio(type = "ratio")
}
}
\keyword{internal}
